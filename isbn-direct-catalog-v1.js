(()=>{
if(window.__LIB_DIRECT_CATALOG_ISBN_V1)return;window.__LIB_DIRECT_CATALOG_ISBN_V1=true;
const prev=window.fetch.bind(window),cache=new Map();
const clean=v=>String(v??'').replace(/[\u200B-\u200F\u202A-\u202E\u2060\u2066-\u2069\uFEFF]/g,'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim();
const norm=v=>clean(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[’‘]/g,"'").replace(/[^a-z0-9]+/g,' ').trim();
const code=v=>String(v||'').replace(/[^0-9Xx]/g,'').toUpperCase();
function isbn13to10(v){const n=code(v);if(!/^978\d{10}$/.test(n))return'';const core=n.slice(3,12);let s=0;for(let i=0;i<9;i++)s+=Number(core[i])*(10-i);const c=(11-s%11)%11;return core+(c===10?'X':String(c))}
function aliases(v){const n=code(v),x=n.length===13?isbn13to10(n):'';return [...new Set([n,x].filter(Boolean))]}
async function fetchText(url,timeout=7000){const c=new AbortController(),t=setTimeout(()=>c.abort(),timeout);try{const r=await prev(url,{signal:c.signal,headers:{Accept:'text/plain,text/html,*/*'}});if(!r.ok)return'';return await r.text()}catch(e){return''}finally{clearTimeout(t)}}
async function reader(url){for(const p of ['https://r.jina.ai/'+url,'https://api.allorigins.win/raw?url='+encodeURIComponent(url),'https://corsproxy.io/?url='+encodeURIComponent(url)]){const x=await fetchText(p);if(x&&x.length>250)return x}return''}
function hostOk(u){try{return /(?:^|\.)(?:bompiani\.it|giunti\.it|giuntialpunto\.it|ibs\.it|libreriauniversitaria\.it|lafeltrinelli\.it|libraccio\.it)$/.test(new URL(u).hostname.replace(/^www\./,''))}catch(e){return false}}
function links(raw){const out=[],seen=new Set(),add=u=>{try{const x=new URL(u);if(!hostOk(x.href)||seen.has(x.href))return;seen.add(x.href);out.push(x.href)}catch(e){}};let m;for(const re of [/\[[^\]]*\]\((https?:\/\/[^)\s]+)\)/g,/(https?:\/\/(?:www\.)?(?:bompiani\.it|giunti\.it|giuntialpunto\.it|ibs\.it|libreriauniversitaria\.it|lafeltrinelli\.it|libraccio\.it)\/[^\s<>)\]]+)/gi])while((m=re.exec(String(raw||''))))add(m[1]);return out}
function exact(raw,isbn){const compact=code(raw);return aliases(isbn).some(x=>x&&compact.includes(x))}
function strip(raw){return clean(String(raw||'').replace(/\[([^\]]+)\]\([^)]*\)/g,'$1').replace(/[*_`#]/g,' '))}
function field(raw,names){const lines=String(raw||'').split(/\r?\n/);for(let i=0;i<lines.length;i++){const x=strip(lines[i]);for(const n of names){const m=x.match(new RegExp('^'+n+'\\s*[:|]?\\s*(.*)$','i'));if(!m)continue;const inline=clean(m[1]);if(inline)return inline;for(let j=i+1;j<Math.min(lines.length,i+4);j++){const y=strip(lines[j]);if(y)return y}}}return''}
function titleOf(raw){let t=field(raw,['Titolo','Title']);if(t)return t;for(const line of String(raw||'').split(/\r?\n/)){const m=line.match(/^\s*#\s+(.+?)\s*$/);if(m){const x=strip(m[1]);if(x&&!/^(?:catalogo|ricerca|search|bompiani|giunti|ibs)$/i.test(x))return x}}return''}
function authorOf(raw,title){let a=field(raw,['Autore','Author']);if(a)return a.replace(/^di\s+/i,'').trim();const lines=String(raw||'').split(/\r?\n/);for(let i=0;i<lines.length;i++){const x=strip(lines[i]);if(norm(x)!==norm(title))continue;for(let j=i+1;j<Math.min(lines.length,i+7);j++){const y=strip(lines[j]).replace(/^di\s+/i,'');if(/^[A-Za-zÀ-ÿ'’.\- ]{4,120}$/.test(y)&&!/(?:editore|collana|descrizione|formato|pagine)/i.test(y))return y}}return''}
function plotOf(raw){const s=String(raw||'');const m=s.match(/(?:^|\n)\s*#{1,5}\s*(?:Descrizione|Trama|Sinossi)[^\n]*\n([\s\S]{80,1800}?)(?=\n\s*#{1,5}\s|\n\s*(?:Dettagli|Recensioni|Conosci l.autore)\b|$)/i);return m?strip(m[1]):''}
function parse(raw,url,isbn){if(!raw||!exact(raw,isbn))return null;let title=titleOf(raw).replace(/\s*[-–—|]\s*(?:Bompiani|Giunti|IBS|Libreria Universitaria).*$/i,'').trim();const author=authorOf(raw,title);if(!title||!author)return null;let publisher=field(raw,['Editore','Publisher']);if(!publisher&&/bompiani\.it/i.test(url))publisher='Bompiani';const date=field(raw,['Data di Pubblicazione','Data di pubblicazione','Anno edizione','In libreria da','Pubblicazione']);const year=(date||raw).match(/\b((?:18|19|20)\d{2})\b/)?.[1]||'';return{title,author,publisher,year,description:plotOf(raw),source:url}}
async function lookup(isbn){const k=code(isbn);if(cache.has(k))return cache.get(k);const p=(async()=>{const q=encodeURIComponent(k),searches=[`https://www.bompiani.it/search?query=${q}`,`https://www.bompiani.it/search?q=${q}`,`https://www.bompiani.it/catalogo?search=${q}`,`https://www.bompiani.it/catalogo?q=${q}`,`https://giunti.it/search?q=${q}`,`https://giunti.it/search?type=product&q=${q}`,`https://www.ibs.it/search?query=${q}`,`https://www.lafeltrinelli.it/search?query=${q}`,`https://www.libreriauniversitaria.it/cerca_libri.php?search=${q}`],targets=[];for(const u of searches){const raw=await reader(u);const direct=parse(raw,u,k);if(direct){window.__LIB_DIRECT_CATALOG_LAST__={isbn:k,url:u,direct:true,rec:direct};return direct}for(const x of links(raw))if(!targets.includes(x))targets.push(x);if(targets.length>=14)break}for(const u of targets.slice(0,14)){const raw=await reader(u),rec=parse(raw,u,k);if(rec){window.__LIB_DIRECT_CATALOG_LAST__={isbn:k,url:u,direct:false,rec};return rec}}window.__LIB_DIRECT_CATALOG_LAST__={isbn:k,rec:null,targets};return null})();cache.set(k,p);return p}
function shape(rec,isbn){const ids=aliases(isbn).map(x=>({type:x.length===10?'ISBN_10':'ISBN_13',identifier:x})),v={title:rec.title,authors:[rec.author],publisher:rec.publisher||'',publishedDate:rec.year||'',language:'it',industryIdentifiers:ids,description:rec.description||'',categories:[]};return{kind:'books#volumes',totalItems:1,items:[{id:'direct-catalog-'+code(isbn),volumeInfo:v,__directCatalogSource:rec.source}]}}
function usable(data,isbn){if(!data?.items?.length)return false;const aa=aliases(isbn);return data.items.some(it=>{const v=it?.volumeInfo||{},ids=(v.industryIdentifiers||[]).map(x=>code(x.identifier));return clean(v.title)&&Array.isArray(v.authors)&&v.authors.length&&(!ids.length||ids.some(x=>aa.includes(x)))})}
window.__LIB_DIRECT_CATALOG_ISBN_LOOKUP=lookup;
window.fetch=async function(input,init){let u;try{u=new URL(typeof input==='string'?input:input.url,location.href)}catch(e){return prev(input,init)}if(!(u.hostname==='www.googleapis.com'&&u.pathname.includes('/books/v1/volumes')))return prev(input,init);const m=(u.searchParams.get('q')||'').match(/^isbn:([0-9Xx-]+)/i);if(!m)return prev(input,init);const isbn=code(m[1]);let original=null;try{original=await prev(input,init);if(original?.ok){const d=await original.clone().json();if(usable(d,isbn))return original}}catch(e){}try{const rec=await lookup(isbn);if(rec)return new Response(JSON.stringify(shape(rec,isbn)),{status:200,headers:{'content-type':'application/json; charset=utf-8'}})}catch(e){window.__LIB_DIRECT_CATALOG_ERROR__=String(e&&e.message||e)}return original||prev(input,init)};
})();