(()=>{
const root=typeof window!=='undefined'?window:globalThis;
if(root.__LIB_SERIES_SAGA_LOCK_V9)return;root.__LIB_SERIES_SAGA_LOCK_V9=true;root.__LIB_SERIES_SAGA_LOCK_V10=true;
const clean=v=>String(v??'').replace(/[\u200B-\u200F\u202A-\u202E\u2060\u2066-\u2069\uFEFF]/g,'').replace(/\u00a0/g,' ').replace(/\s+/g,' ').trim();
const norm=v=>clean(v).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[’‘]/g,"'").replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const code=v=>String(v||'').replace(/[^0-9Xx]/g,'').toUpperCase();
const MAX_TRIES=12;
function sameTitle(a,b){const x=norm(a),y=norm(b);return !!x&&!!y&&(x===y||(x.length>=8&&y.startsWith(x+' '))||(y.length>=8&&x.startsWith(y+' ')))}
function input(){if(typeof document==='undefined')return{code:'',title:'',author:''};const g=id=>clean(document.getElementById(id)?.value||'');return{code:code(g('editCode')),title:g('editTitle'),author:g('editAuthor')}}
function sameInput(a,b){return a.code===b.code&&sameTitle(a.title,b.title)&&norm(a.author)===norm(b.author)}
function relationShape(r){if(!r?.authoritative||!clean(r.saga))return false;const pre=clean(r.prequel),seq=clean(r.sequel);if(r.initial&&r.terminal)return true;if(r.initial)return !!seq;if(r.terminal)return !!pre;return !!pre&&!!seq}
function localized(r){if(!relationShape(r)||r.localizationPending===true)return false;const pre=clean(r.prequel),seq=clean(r.sequel);if(pre&&r.localizedPrequel!==true)return false;if(seq&&r.localizedSequel!==true)return false;return true}
let active=false,stable=null,inflight=false,lastTry=0,tries=0,exhausted=false;const manual=new Set();
function state(reason=''){const i=input();root.__LIB_SERIES_SAGA_LOCK_STATE__={active,code:i.code,title:i.title,author:i.author,pending:!!active&&!stable&&!exhausted,settled:!!stable||exhausted||!active,complete:!!stable,exhausted,tries,reason,at:Date.now()}}
function setAuto(id,value,allowEmpty=false){const el=document.getElementById(id);if(!el||manual.has(id))return;const v=clean(value);if(!v&&!allowEmpty)return;if(el.value===v)return;el.value=v;el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}))}
function suppressCanonical(i,r){if(!active||!r||!sameInput(input(),i))return;for(const [id,key,flag] of [['editPrequel','prequel','localizedPrequel'],['editSequel','sequel','localizedSequel']]){const v=clean(r[key]);if(!v||r[flag]===true)continue;const el=document.getElementById(id);if(el&&norm(el.value)===norm(v))setAuto(id,'',true)}}
function apply(){if(!active||!stable||!sameInput(input(),stable.input))return;const r=stable.result;if(r.saga)setAuto('editSaga',r.saga);if(r.prequel)setAuto('editPrequel',r.prequel);else if(r.initial)setAuto('editPrequel','',true);if(r.sequel)setAuto('editSequel',r.sequel);else if(r.terminal)setAuto('editSequel','',true)}
function remember(i,r){if(!r||!sameInput(input(),i))return false;if(!localized(r)){suppressCanonical(i,r);state('waiting-localization');return false}stable={input:i,result:r};exhausted=false;apply();root.__LIB_SERIES_SAGA_LOCK_LAST__={input:i,result:r,at:Date.now()};state('localized');return true}
function consumeKnown(){const i=input(),candidates=[root.__LIB_SERIES_V8_APPLIED?.result,root.__LIB_LAST_AUTHORITATIVE_SERIES_RESULT__,root.__LIB_LAST_UNIVERSAL_SERIES_RESULT__,root.__LIB_LAST_BOUNDED_RELATIONS_RESULT__];for(const r of candidates)if(r&&remember(i,r))return true;return false}
async function resolve(){if(!active||inflight||stable)return;const i=input();if(!i.code||!i.title||!i.author){state('waiting-metadata');return}if(consumeKnown())return;const now=Date.now();if(now-lastTry<3800)return;if(tries>=MAX_TRIES){exhausted=true;state('exhausted');return}const fn=root.__LIB_RESOLVE_AUTHORITATIVE_SERIES_NEIGHBORS;if(typeof fn!=='function'){state('waiting-resolver');return}lastTry=now;tries++;inflight=true;state('resolving');try{const r=await Promise.resolve(fn({code:i.code,title:i.title,author:i.author,saga:clean(document.getElementById('editSaga')?.value||'')}));remember(i,r)}catch(e){root.__LIB_SERIES_SAGA_LOCK_ERROR__=String(e&&e.message||e)}finally{inflight=false;if(!stable&&tries>=MAX_TRIES){exhausted=true;state('exhausted')}else state(stable?'localized':'pending')}}
function activate(){active=true;stable=null;inflight=false;lastTry=0;tries=0;exhausted=false;manual.clear();state('activated');consumeKnown();setTimeout(resolve,300)}
function boot(){if(typeof document==='undefined')return;const dlg=document.getElementById('editDialog');if(!dlg){setTimeout(boot,120);return}document.addEventListener('click',e=>{if(e.target?.id==='lookupMetadataBtn')activate()},true);for(const id of ['editSaga','editPrequel','editSequel'])document.getElementById(id)?.addEventListener('input',e=>{if(e.isTrusted){manual.add(id);state('manual')}});for(const id of ['editCode','editTitle','editAuthor']){const el=document.getElementById(id);el?.addEventListener('input',()=>{if(dlg.open)activate()});el?.addEventListener('change',()=>{if(dlg.open)activate()})}new MutationObserver(()=>{if(dlg.open)activate();else{active=false;stable=null;manual.clear();exhausted=false;state('closed')}}).observe(dlg,{attributes:true,attributeFilter:['open']});if(dlg.open)activate();setInterval(()=>{if(!active)return;if(!stable)consumeKnown();apply();resolve()},700)}
boot();
root.__LIB_SERIES_SAGA_LOCK_TEST__={relationShape,localized,sameInput,consumeKnown,apply,resolve};
})();
