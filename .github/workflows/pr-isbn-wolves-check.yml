name: PR metadata and series check

on:
  pull_request:
    branches: [main]
    paths:
      - 'series-neighbors-standalone-v5.js'
      - 'series-authoritative-runtime-v6.js'
      - 'series-single-owner-guard-v1.js'
      - 'storygraph-goodreads-genres-v3.js'
      - 'isbn-field-sanitizer-v1.js'
      - 'isbn-cover.js'
      - 'scripts/test-series-regressions-v6.js'
      - '.github/workflows/cache-bust-runtime-v1.yml'
      - '.github/workflows/pages.yml'
      - '.github/workflows/live-isbn-regression-v1.yml'
      - '.github/workflows/pr-isbn-wolves-check.yml'
      - '.nojekyll'

permissions:
  contents: read

concurrency:
  group: pr-metadata-series-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  static:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Sintassi e configurazione runtime
        run: |
          node --check storygraph-goodreads-genres-v3.js
          node --check series-neighbors-standalone-v5.js
          node --check series-authoritative-runtime-v6.js
          node --check series-single-owner-guard-v1.js
          node --check isbn-field-sanitizer-v1.js
          node --check scripts/test-series-regressions-v6.js
          grep -q '__LIB_SERIES_NEIGHBORS_STANDALONE_V46' series-neighbors-standalone-v5.js
          grep -q 'series-authoritative-runtime-v6.js?v=20260820-10' series-neighbors-standalone-v5.js
          grep -q 'series-single-owner-guard-v1.js?v=20260820-4' series-neighbors-standalone-v5.js
          grep -q 'isbn-field-sanitizer-v1.js?v=20260820-1' series-neighbors-standalone-v5.js
          grep -q 'single-owner-goodreads-complete-then-storygraph-then-wikipedia-v6' series-neighbors-standalone-v5.js
          grep -q 'goodreads-ordered-series-primary-v6' series-authoritative-runtime-v6.js
          grep -q 'storygraph-series-completion-v1' series-authoritative-runtime-v6.js
          grep -q '__LIB_ISBN_FIELD_SANITIZER_V1' isbn-field-sanitizer-v1.js
          grep -q 'storygraph-goodreads-genres-v3.js?v=20260820-1' series-neighbors-standalone-v5.js
          ! grep -q 'series-authoritative-runtime-v5.js' series-neighbors-standalone-v5.js
          ! grep -q '9788845279553' series-authoritative-runtime-v6.js
          ! grep -q 'Simon Beckett' series-authoritative-runtime-v6.js
          ! grep -q 'David Hunter' series-authoritative-runtime-v6.js
          ! grep -q '9788868363697' series-authoritative-runtime-v6.js
          ! grep -q '9788868363710' series-authoritative-runtime-v6.js
          ! grep -q 'Stephen King' series-authoritative-runtime-v6.js
          ! grep -q 'La Torre Nera' series-authoritative-runtime-v6.js
          ! grep -q 'Alexandra Adornetto' series-authoritative-runtime-v6.js
          ! grep -q 'Sacrifice' series-authoritative-runtime-v6.js
          grep -q "Forza cache runtime nell'artefatto" .github/workflows/pages.yml
          grep -q 'GITHUB_SHA' .github/workflows/pages.yml
          test -f .nojekyll
          ! grep -Eq '^[[:space:]]*git[[:space:]]+push\b' .github/workflows/cache-bust-runtime-v1.yml
      - name: Verifica parser StoryGraph generi
        run: |
          node <<'NODE'
          const fs=require('fs'),vm=require('vm');
          global.window=global;
          global.document={getElementById(){return null}};
          global.setInterval=()=>0;global.clearInterval=()=>{};global.setTimeout=()=>0;global.clearTimeout=()=>{};
          vm.runInThisContext(fs.readFileSync('storygraph-goodreads-genres-v3.js','utf8'));
          const got=window.__LIB_STORYGRAPH_GENRES_TEST__.storyGenresFromLine('fiction fantasy horror adventurous dark mysterious medium-paced');
          if(JSON.stringify(got)!==JSON.stringify(['Fantasy','Horror']))throw new Error('Generi inattesi: '+JSON.stringify(got));
          console.log('Generi:',got.join(', '));
          NODE

  integration:
    needs: static
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Installa Playwright
        run: |
          npm init -y >/dev/null 2>&1
          npm install playwright@1.55.0 >/dev/null 2>&1
          npx playwright install --with-deps chromium >/dev/null
      - name: Avvia sito locale della PR
        run: |
          python3 -m http.server 8000 --bind 127.0.0.1 >/tmp/pr-http.log 2>&1 &
          echo $! >/tmp/pr-http.pid
          for i in $(seq 1 20); do
            curl -fsS http://127.0.0.1:8000/ >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/pr-http.log
          exit 1
      - name: Test relazioni V6 e pulizia metadati
        run: node scripts/test-series-regressions-v6.js
